# 换行策略：dist 产物禁止任何 EOL 转换，必须字节级可复现。
#
# 背景（2026-09-20 实测）：本仓库 core.autocrlf=true，且此前没有 .gitattributes，
# 于是 LF blob 在检出时被改写成 CRLF —— 一个干净 clone 得到的 frontend/dist 与构建机
# 字节不一致：42 个文件（19 js / 18 css / svg / md / txt / .gitkeep）被改写；
# index.html 更明显：构建机上是「上游 CRLF + 注入 LF」的混合（1752 CRLF + 1123 纯 LF），
# 但 blob 被归一化成纯 LF，clone 又检出成纯 CRLF（2875 个 CRLF，多出 1123 字节）。
#
# 为什么必须钉死：
#   1) 「测过的产物 == 发布的产物」：任何字节差异都让本地实测结论对 clone 不成立；
#   2) 注入/品牌脚本按字符串锚点改写 index.html，锚点形态越确定越安全；
#   3) 构建机与 CI/clone 的 dist 必须能逐字节比对（verify-dist-assets.js 只查存在性与
#      跟踪状态，字节一致性靠本规则 + 人工 diff 保证）。
#
# -text = 关闭 EOL 转换（仍保留文本 diff，不是 binary）。dist 现存字节即权威版本：
# 上游产物的混合换行原样入库，不再被任何一侧悄悄改写。
frontend/dist/** -text

# 构建输入：被内联进 dist/index.html 的载荷脚本必须 LF（钉住，不随检出设置变化）。
#
# 为什么：注入结果 = 载荷脚本的逐字节内联，所以载荷的换行会直接进入发布的 index.html。
# 实测（2026-09-20）：构建机上 dsh-update-banner.js / injections.json 是 LF（工具写出），
# 而 clone 在 core.autocrlf=true 下检出成 CRLF → 同一次重放产出多出 1015 个 CRLF 的
# index.html。内容逐字符一致，但「clone 重放 == 提交产物」就无法用字节比对来验证了。
# 钉成 LF 后，任何平台/任何 autocrlf 设置的 clone 重放结果都相同。
scripts/dsh-*.js text eol=lf
scripts/injections.json text eol=lf

# 同理：品牌资产里被内联进 index.html 的两个文本源（apply-branding.js 读取后写入 dist）。
# 实测：在 core.autocrlf=false 的 clone 里重放会把这 8 行写成 LF，使 index.html 比构建机
# 少 8 字节（内容逐字符一致）。钉住后两种 autocrlf 设置产出相同字节。
branding/brand.css text eol=lf
branding/head-error-hook.js text eol=lf
