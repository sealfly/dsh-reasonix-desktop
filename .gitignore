# 构建产物
build/
# 自定义 NSIS 安装器脚本（build-installer.ps1 依赖，含 DSH 后端组件选项，必须入库）
#
# 注意：git 不允许「父目录被排除后再重新包含其子文件」，所以直接写
# !build/windows/installer/ 是**死规则**——实测 `git add build/windows/installer/project.nsi`
# 会被静默跳过（2026-09-20 再次踩到：BOM/路径修复差点没进提交）。必须逐级
# 「排除本级内容 → 放行下一级目录」，最后才放行具体文件。
build/*
!build/windows/
build/windows/*
!build/windows/installer/
build/windows/installer/*
!build/windows/installer/project.nsi
!build/windows/installer/start-dsh.cmd
# 前端 dist（从 Electron 项目 renderer/dist 复制）
# 必须入库，禁止忽略：Wails 壳在构建时直接嵌入 frontend/dist，干净 checkout 缺任何一个
# chunk 都会让首屏永久卡在「加载中」。历史上这里忽略过 frontend/dist/，导致 1.38.2 升级
# 提交（d72a110）静默丢失 415 个资产（含 assets/monaco 与 provider-icons）。
# 新增 chunk 必须以 git add 入库，由 scripts/verify-dist-assets.js 门禁强制校验。
# Wails 自动生成的绑定（每次 wails build 重新生成）
frontend/wailsjs/
# 可执行文件
*.exe
# 诊断截图/日志
*.png
*.log
# 便携工具（gh.exe 等）
.tools/

# 品牌改造资产（apply-branding.js 的输入，必须入库：否则换机器/重装后构建会丢 DSH 品牌）
!branding/
!branding/*.png
